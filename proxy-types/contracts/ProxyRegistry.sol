// SPDX-License-Identifier: MIT
pragma solidity >= 0.6.0 < 0.8.0;

// WIP
contract ProxyRegistry {
    // mapping(address => address) public implementations;

    // function setImplementation(address proxy, address implementation) public {
    //     implementations[proxy] = implementation;
    // }

    // function getImplementation(address proxy) public view returns (address) {
    //     return implementations[proxy];
    // }

    // fallback() public view returns (address) {
    //     return getImplementation(msg.sender);
    // }
}
